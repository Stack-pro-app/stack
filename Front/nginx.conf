server {
    listen 80;
    

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

  

    location /api/auth/ {
        proxy_pass http://stack-app.test.auth-service:8080/;
    }

    

}
